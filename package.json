{
  "name": "winput-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "type": "module",
  "scripts": {
    "build": "bun run --filter '*' clean && bun run --filter '*' build",
    "test": "bun test",
    "commit": "bun run build && bun run scripts/commit.ts",
    "release": "bun run build && bun run scripts/release.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/cli-progress": "^3.11.6",
    "cli-progress": "^3.12.0",
    "concurrently": "^9.2.1",
    "gradient-string": "^3.0.0",
    "nanospinner": "^1.2.2",
    "typescript": "^5.9.3"
  }
}
