{
  "name": "winput-monorepo",
  "private": true,
  "workspaces": ["packages/*"],
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "build:force": "bun run scripts/build.ts --force",
    "build:all": "bun run --filter '*' clean && bun run --filter '*' build",
    "test": "bun test",
    "commit": "bun run build && bun run scripts/commit.ts",
    "release": "bun run build && bun run scripts/release.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "^1.3.5",
    "gradient-string": "^3.0.0",
    "nanospinner": "^1.2.2",
    "typescript": "^5.9.3"
  }
}
